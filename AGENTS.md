# Painter cloud task

This branch stages the painter teacher benchmark in Codex cloud. Follow
`painter/benchmarks/openrouter-teachers-20260922/CODEX_CLOUD.md`.

Do not read, print, log, commit, or include API tokens in artifacts. The paid
runner reads the environment; do not inspect credential variables or files.
Keep `episodes/`, screenshots, generated programs, model output, logs, browser
caches, and reference JPEGs out of Git. Store completed benchmark evidence in
the public Hugging Face dataset through the hash-verifying publisher.

Start with one episode and verify its public HF receipt before scaling. Do not
start scheduled tasks or watchers. Do not stop or modify the separate rented
node's ongoing run.
