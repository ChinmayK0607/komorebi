Paint the supplied reference as an attractive, recognizable painting using p5.js 2.3.2 and p5.brush 2.2.1 JavaScript on a 600x600 WEBGL canvas. In draw(), translate(-300,-300) once so (0,0) is top left. Use setup() and draw(), and end draw() with noLoop().

First get the large shapes, silhouette, scale, placement, and value relationships right. For opaque primary forms, use p5's native noStroke(); fill(color); beginShape(); vertex(x,y); endShape(CLOSE), or ellipse/rect. These are reliable solid underpaint. Use p5.brush for painterly edge variation, translucent overlays, texture, and selective marks above that underpaint. A reference object must never look transparent because its base was painted only with brush.fill(). Preserve the silhouette when revising.

Valid p5.brush names are pen, rotring, 2B, HB, 2H, cpencil, pastel, crayon, charcoal, spray, and marker. Call brush.load() in setup(). Do not call brush.init() or invent brushes. For a brush line: brush.noFill(); brush.noWash(); brush.set('cpencil','#554d44',0.8); brush.line(x1,y1,x2,y2). For a translucent polygon: brush.noStroke(); brush.noWash(); brush.fill('#688a9b',100); brush.polygon(points); brush.noFill(). Reset inactive modes when switching.

Keep loops finite and modest. Avoid dense uniform hatching, grid textures, and random marks that compete with the subject. Spend detail where it improves likeness or beauty. No embedded images, pixel copies, labels standing in for objects, or external resources. There is no stroke or speed budget in this quality phase.

Inspect the reference and current canvas. Return a brief visual plan and a complete sketch inside one javascript code block. You will receive the actual render to revise. After submitting code, inspect the next CURRENT CANVAS and fix its most important mismatch. Do not append FINISHED to a code response. Only return FINISHED with no code after observing a current canvas that already matches the reference well. Do not repeat unchanged code as a revision.
