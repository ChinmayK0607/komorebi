# Reviewed multi-turn painting SFT — preparation, 2026-09-25

**Completed result:** [Wave-4 matched evaluation](MULTITURN_RESULT_WAVE4.md) does not support promoting the multi-turn adapter: step 24 and step 40 each yielded 12/28 valid final canvases versus 17/28 for the matched photo-SFT step-512 initializer, and visual inspection favored the initializer overall. The public evidence archive is hash-verified, and the GPU node has been released. The preparation and launch details below describe the completed experiment; forward-looking statements should be read in light of this result.

## Wave-4 launch record (supersedes provisional data/setup status below)

The finite nine-photo MiMo Pro teacher wave is publicly archived at HF dataset `CK0607/komorebi-painter-teachers` revision `fab82ab46d94becf279d973c99989328364c3ba8`, archive SHA-256 `0f65ab1cca0a1b639657355d02ed47d1e010135d9d2bc660df092635249d1c35`, from source commit `862c28929151e62dfcedb2823fadc4e430ae7ad2`. It made 36 Gateway requests and reported 830,003 total tokens; all 36 provider cost fields are missing, so the recorded zero subtotal is **not** a measured free run. The [admission decisions](turn-admissions-wave4.json) add 11 visually reviewed, renderer-valid, contract-valid turns from six photo scenes and reject invalid/regressive turns. Across prior simple/reused scenes and this wave, there are 38 approved targets over 16 scenes. All their reference IDs are training sources. The frozen 28-photo development manifest is disjoint by exact reference hash.

The exporter preserves the teacher's complete target program and full-resolution source evidence, but embeds 448-pixel-max reference/canvas training views. To fit the 16,384-token window, revision rows include the previous rendered canvas and outcome/renderer feedback, not the full prior program. This is **visual-state-conditioned revision SFT**, not imitation of every raw assistant turn. Invalid or worse programs are never assistant targets. Reviewed JSONL SHA-256: `6de85c426673cae3d81003d84cdf5991d2c7e4288e2fbfc2c4912ccb5e6d0029`. The final deterministic mix SHA-256 is `e1a67162d7f40350a1fbd072d7ef4fcdd971de280d6e37ebfaa0a5ff671e1891`: 120 reviewed exposures (three repeats plus six explicit padding exposures) and 40 procedural-retention exposures, 160 total rows, 40 optimizer updates, eight-update checkpoint cadence, 36 separate procedural validation rows. Checkpoint padding is 6/114 planned reviewed exposures (5.3%). The pinned Prime processor checked every training and validation row, exact assistant-only mask and no truncation; maximum processed length is 10,220 tokens. A two-GPU RTX PRO 6000 Blackwell 96 GB node was rented at a listed $2.38/hour; GPU 0 trains while GPU 1 runs the matched step-512 baseline, then the final public checkpoint is evaluated on the same 28 cases. Measure cost from rental elapsed time and utilization over the run window; do not infer saturation from a single GPU snapshot.

The first launch attempt stopped before an optimizer update: shell `read` returned failure on a non-newline-terminated protected token file, and the eval process lacked the native taskset on `PYTHONPATH`. Both launch scripts were corrected and the failed attempt's logs/exit statuses preserved separately on the node. The second finite campaign finished all 40 SFT updates with exit 0. The public, hash-verified final adapter is [step 40](https://huggingface.co/CK0607/qwen3.8-27b-brush-painting/tree/e80af8c90f9dfa2d7a22696dbaf9aaed3d30c8ae/multiturn-sft-20260925-v1/step_40), weight SHA-256 `b026b2d4d9ca4d77ffdc8db856d7c72f385af44ca3aa8273ff7fc31fec4443e3`. Procedural validation loss fell from 0.2179 at the start to 0.1842 at step 40; this is training evidence, not a visual-quality result. During the bounded 14:46:32–15:07:58 UTC training window, 129 ten-second utilization samples per GPU averaged 73.3% on trainer GPU 0 and 93.8% on concurrent baseline-eval GPU 1; the busy fractions at ≥10% were 86.0% and 93.8%. The matched baseline completed 28/28 cases in 2,056 seconds; raw final-canvas validity is 17/28, with 24 cases attempting two turns. Its visual quality is unjudged. The final trained evaluation runs on freed GPU 0 while an optional public step-24 midpoint evaluation runs on GPU 1, to check for a better intermediate checkpoint without adding idle-GPU rental time. The [small evidence bundler](collect_multiturn_result.py) verifies the public final checkpoint and completed baseline/final eval receipts before collecting traces, PNGs, manifests, logs and telemetry; it also includes the midpoint evidence if available, and excludes credentials, caches and weights. No visual improvement claim or checkpoint promotion is made yet.

**Hypothesis.** Starting from the public Qwen3.8-27B photo SFT step-512 adapter, supervised revisions conditioned on the *actual* rendered canvas will improve held-out painting quality and repair behavior. The matched baseline is the same step-512 adapter, base model `Qwen/Qwen3.8-27B` revision `1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0`, adapter revision `adf687e5b321514eda7be6eab050434fdc8e44ff`, adapter-weight SHA-256 `bea72b066986faa1b92b9dc539c710093181ee916b99323e2b2498f318ac1740`. Compare under the identical inference scaffold, render settings, seeds and turn budget. The fixed photo holdout has 28 cases, manifest SHA-256 `9943878cc43d48f1cf9ff60e5af633089bf2bd8d45141216531363cad8f1c878`. The campaign now allows two model turns per case so the same rollouts expose first-paint quality, whether a valid next turn improves the canvas, and final quality. This is a matched policy-continuation test; because each model sees its *own* first canvas, it does not isolate correction skill on an identical draft. A separate frozen-draft correction test would be needed for that causal claim. Do not confuse training loss with visual progress.

The first parent-reviewed admission file is [turn-admissions-wave1.json](turn-admissions-wave1.json). It selects 15 targets across cup, poppy, sailboat, cottage and the already-paid cake probe. User feedback accepted the general easy-scene painting quality but did not label exact turns; these are parent visual selections, not human consensus. The [exporter](export_turn_sft.py) checks the train-reference hash, turn receipt, program hash, PNG hash and current canvas-pixel contract. Its current 15-row output SHA-256 is `ecf47ec458cc8d5db1251902be20dfe20a4c74c78e62a9c020871e42b492e692`. Invalid turns and regressive turns remain context when useful: the immediately preceding failed sketch and renderer error are shown for repair, but are never assistant loss targets. Pear T3 is visually useful but violates the current coordinate contract; T4 is visibly off-canvas, so neither is supervised. Keep the complete original public teacher evidence.

The finite [diverse cloud wave](https://chatgpt.com/codex/cloud/tasks/task_e_6ab4d051e208832b8240d3e2794edd43) is underway from [Git commit `862c289`](https://github.com/ChinmayK0607/komorebi/commit/862c28929151e62dfcedb2823fadc4e430ae7ad2). It covers two remaining simple train references (orange and fern) and nine new COCO128 train photos (animals, vehicles, objects, relationships), with four turns maximum per scene and at most 44 MiMo Pro requests. Cake is omitted because its six-turn probe already exists. The verified public source bundle is dataset revision `22e3926052e6a0de4f1a8dbc0fcfbda2e12d2507`, SHA-256 `31e6f2aba39ca64b9cdbbb0e8a5ff8dc1cac1de4d39394517d7e3e34dd640e59`. Dry runs selected exactly 11 references, with no benchmark-holdout overlap or paid calls. Generated turns are **candidates** until their rendered images are reviewed. The first two-scene archive is public/hash-verified with SHA-256 `cbca5caeeb9c865865debb720f48b6b4ffe2f2ff59bed68a9081bef488815c1a`. [Wave-2 admissions](turn-admissions-wave2.json) add orange T1/T4 to the initial 15 rows (17 total; export SHA-256 `0bc12a747af03e558bade76593f84228e24658b0b19d1470e9ebc16ff3cf7364`). Fern T2/T3 looked coherent, but the teacher used nested per-leaflet transforms forbidden by the current canvas-pixel contract, so they remain evidence rather than SFT targets. The provisional 17-row mix loaded as 96 train and 36 validation rows through the exact pinned scheduled-data loader; the tokenizer/processor audit still requires the Linux node. MiMo Pro's [listed AI Gateway rate](https://vercel.com/ai-gateway/models/mimo-v2.6-pro) was $0.44/M input and $0.87/M output tokens when checked; actual per-turn costs have been missing in prior Gateway records and must not be reported as zero.

To add diversity without new provider calls, [public archive collector](collect_paid_mimo_reuse.py) recovered four already-paid MiMo benchmark episodes after verifying each public archive's source commit, complete byte count and SHA-256. The parent visually reviewed every rendered turn for giraffes, birds, a waterfront and a bus. [Wave-3 admissions](turn-admissions-wave3.json) select 10 additional targets over those four scenes, bringing the provisional mix to 27 targets across 10 scenes; [reference-manifest-wave3.json](reference-manifest-wave3.json) records the exact added reference hashes. Blank first canvases, erased birds, invalid later turns and near-duplicate revisions are excluded. The wave-3 export SHA-256 is `af6f88744eae62144f4c85c4a2d55ae7be2a51a498e8e2fa860de624aae661f9`; a provisional four-repeat/eight-update-boundary mix has 160 train rows, 36 validation rows, 40 optimizer steps and 12 explicit padding exposures (11.1% of planned reviewed exposures). Its train SHA-256 is `0c9a571096ce442c5fbc34b0d9ae4213d47cfa0948f6022868994093ce1ef560`. Both splits loaded through the pinned exact-data loader. These four teacher-selection reference IDs are now training sources and must not be presented as held-out student benchmark cases. The frozen 28-photo development set remains disjoint. This is still preparation, not a trained-model result.

The [mixture builder](build_multiturn_mix.py) creates a finite ordered data file: three reviewed painting turns and one procedural-retention example per four-row optimizer group. Its first pass places earlier turns before later corrections across scenes; later passes shuffle deterministically. Every training row has exactly one assistant target, and the existing Prime strict processor will reject truncation and audit the generation-prefix mask before updates. The earlier 15-target smoke mix reconstructed 128 rows/32 updates from 15 reviewed and 108 older procedural source rows, with 36 procedural validation rows; it was only a format test. The wave-3 mix above is also provisional until the in-progress nine-photo archive is reviewed. Rebuild it afterward and record the final input and output hashes before any GPU update.

The intended bounded SFT uses the pinned Prime revision `26b3131d2716a4f8210b165df584b83b4bc54f61`, LoRA rank 16/alpha 32, frozen vision encoder, batch four, 16,384-token no-truncation limit and learning rate `3e-6`. The mix manifest sets an eight- or sixteen-update public checkpoint cadence according to final data size, and records the number and fraction of repeated examples needed to reach the final checkpoint boundary. The provisional 17-row mix needs only four padding exposures at an eight-update cadence (5.9% of its planned reviewed exposures). The exact optimizer-step count comes from the frozen mix manifest. The [one-shot node setup](setup_multiturn_node.sh), [trainer entrypoint](train_multiturn.py), [finite campaign launcher](run_multiturn_campaign.sh) and [credential-free payload packager](package_multiturn_node.py) are prepared; source and mixed data rebuilds have passed syntax, hash and shape checks. The verified step-512 initializer is loaded with a fresh optimizer. A two-GPU 96 GB node was listed by Lium at $2.38/hour on 2026-09-25, versus $3/hour for one H200. On two GPUs the one-rank trainer uses GPU 0 while the [same-manifest baseline evaluator](launch_multiturn_eval.sh) uses GPU 1; on one GPU the same launcher performs training and both evaluations sequentially. The trained adapter is evaluated only after its public final checkpoint exists. The setup also installs the eval renderer and verifies all 28 development-reference hashes before inference. A baseline eval failure does not cancel training or public checkpoint export. No node has been rented and no training result exists yet; recheck availability and price at launch. The final adapter is promoted only if blinded matched visual review improves over step-512 without a significant validity or structure regression. Record failed/censored cases, actual elapsed GPU use, cost and the resulting decision.
