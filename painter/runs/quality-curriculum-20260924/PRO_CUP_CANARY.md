# Pro cup canary — 2026-09-25

**Question.** Can MiMo V2.6 Pro, using the same solid-underpaint prompt and simple-cup reference as the completed MiMo Flash canary, produce a visibly better valid painting within four turns? This is a teacher-scaffold decision, not a training run.

**Matched evidence.** The Flash canary used source `7ec3c7d`, the same reference SHA-256 `c6a862a0b281fa693ceca9b6a34a092b025a8721e68696083441179cde95341b` and the same `quality-prompt.txt`. Its turn-4 canvas was valid but omitted the cup; turns 5 and 6 were invalid. It consumed 166,935 reported tokens over six turns, with missing provider cost fields. The separate Pro cake probe on a harder COCO image produced a recognizable but mechanical six-turn canvas and consumed 203,969 reported tokens. Neither trajectory is admitted to SFT.

**Canary.** `probe_mimo_pro_cup_cloud.sh` uses one verified training reference, the unchanged solid-underpaint prompt, four turns maximum, 16,384 completion tokens per turn, no automatic provider retries, a 900-second request deadline, a 240-second render deadline, and a 2,400-second episode deadline. It runs on Codex Cloud CPU and publishes a public, anonymously hash-verified archive under `mimo-pro-cup-opaque-20260925`. No Lium GPU or scheduled task is used. The source commit and final archive revision must be recorded after execution.

**Decision.** Inspect reference, all actual renders and code. Require a recognizable opaque cup, attached handle, coherent table/wall composition and pleasant painterly finish. If Pro cannot clear this easy case within the bound, stop scaling the current full-sketch-per-turn scaffold and change the action representation or teacher before buying a large curriculum. A valid program or a turn-limit status alone is not acceptance.

**Observed result.** From source `41da4ba85c0ca226c42a2d4621d8b99f4cf9dfe2`, the canary completed four provider requests and no retries in about 11m34s. It reported 27,582 prompt and 34,451 completion tokens; every provider cost field was missing. The public archive `mimo-pro-cup-opaque-20260925` at dataset revision `c9e32bfbfe882c59cc102950ca836f1157a4a884` has SHA-256 `48d2ccdb34b8b4fcf551a7f4d52c7a27fe0980a294e813afa72977faa85c1109` and passed anonymous download/hash verification. Turn 1 exhausted its 16,384-token completion cap without a program. Turns 2–4 each generated code but failed with `Cannot read properties of undefined (reading 'map')`; their partial canvases have background, table, shadow and handle but no cup body. The result is **invalid and rejected for SFT**. A CPU-only replay replaced exactly 13 `bezierVertex` calls in turn 4 with straight `vertex` endpoints. With the same protected `runuser -u painter` renderer, the patched program produced a finished valid 600×600 canvas in 0.81 seconds, with no errors or blocked network requests. Input SHA-256 was `743d56f4cb540b1d87f7830d1ac13577ad179dd6cc227cce6c5b4853fa0740c4`; diagnostic PNG SHA-256 was `06eec408333ede2cd09656afc857cb60dec08a86651f23ab813f9fc3fca133e4`. Thus the curved native shape path, rather than provider delivery or a generic Chromium failure, caused the repeated invalid renders. The edited program restores a recognizable cup, handle and shadow, but its handle is angular, wall/table are flat and straight hatching remains mechanical. It fails the quality-first visual admission bar and stays diagnostic evidence only. No larger batch should launch from this scaffold.

**One repair canary before any scale decision.** A fresh isolated stage `easy-pro-cup-curve-v2` and public run ID `mimo-pro-cup-repair-v2` will test the revised prompt on the identical simple-cup reference. The prompt explains the observed curve failure and asks for smooth contours via sampled vertices, soft varied edges, and fewer diagram-like outlines; invalid-render feedback names the curve repair if the model repeats it. Four turns, at most 20,480 completion tokens per turn, one provider request per turn, no retries, 900-second request and 240-second renderer deadlines bound the cost. Compare the actual final canvas with the same reference, the rejected unedited canary, and the repaired-but-flat diagnostic. Require a valid, recognizable cup *and* an aesthetically better finish before accepting any trajectory. If it remains mechanical or invalid, stop this full-sketch-per-turn scaffold and change the action representation rather than buy a larger teacher curriculum.

**Repair outcome.** Source `40fd21b432df782cc33e90d5b2778f37d430b832`, prompt SHA-256 `4c602cb64e0272dda937fb6b55b80a70728f80927c7cdb38b7ff8214c85850b1`, stage config SHA-256 `f5bad9fcbf25d982d5253fdea130801d7dc427747b1c5222eed8a18bd68fe3bb`, reference SHA-256 `c6a862a0b281fa693ceca9b6a34a092b025a8721e68696083441179cde95341b`. MiMo Pro's provider revision was not exposed. Four of four turns generated programs and valid canvases, with four Gateway requests and no retries. The run took about 14m50s and reported 43,888 prompt plus 43,359 completion tokens (87,247 total). Every per-turn cost field was missing; aggregate `0.0` is not a free-run claim. The public evidence archive at dataset commit `ffd71c37a8b2d1136a3421526b95ea162bbd7ecd` is 299,927 bytes, SHA-256 `05021765f752b83fa36ac8fed82a20293e69b1a9249b0d54a7b90ae4d8b118c6`; anonymous reconstruction verified. This establishes that the curve-safe instruction repaired execution, compared with zero valid turns in the matched prior Pro cup canary. Visual admission still fails: turn 2 improves the handle, turn 3 adds harsh light wedges and striping, and turn 4 removes much of that lighting. The final cup is recognizable but flat, with schematic wood grain and weak ceramic form. No trajectory is admitted to teacher SFT, and no larger batch is launched.

**Second isolated diagnostic.** Turn-4 code called `brush.fill(...)` 34 times before a `poly()` helper implemented with p5 native `beginShape()/vertex()`, and called `brush.polygon()` zero times. Those brush fills do not color native polygons. A CPU-only replay changed only those 34 calls to native `fill(r,g,b,alpha)`, with transformed source SHA-256 `53028557a43e410049c084a6bf3159339ed80ac1468ead522f9d1599f39a215d`. It rendered valid in the same protected Linux wrapper in 0.746 seconds, with no model calls or renderer errors; PNG SHA-256 `24b996767335bc14f518884ebc30cb4319efcca2d38907172aa4141a3f9a9cbc`. The public diagnostic archive at dataset commit `c6e129e1d66bcd1754795dde0ed7b8ea35d7ef23` is 65,088 bytes, SHA-256 `88f0e4f739019e49a1a1688da15fe535ec28d77dcd21fa856f4ccd9a19c2f942`, and independently restored artifact hashes matched. Lighting and body shading appear, confirming this separate API-mixing failure, but the image remains too geometric for admission. The next source revision makes the API distinction explicit and gives targeted feedback when a native `poly()` follows `brush.fill()`; a future paid canary must still show a visual gain before curriculum scale.

**Human preference update.** After seeing the side-by-side four-turn review, the user said the paintings are good and that they like them. This supersedes the parent's stricter subjective rejection as the quality target for the next easy-scene collection. It does not identify a preferred turn or establish quality on other categories. Preserve all four valid cup turns with that provenance; select individual demonstration targets only after reviewing their exact canvases. The native-fill diagnostic remains an edited control, not a model-produced demonstration.
